rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Placeholder deny-all — regole complete in Fase 1 (Step 1.2)
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
